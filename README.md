# <p align="center">Test Technique - Klaire</p>

> Mini WebApp permettant de suivre l'état de documents.

### Usage

```bash
# Un terminal pour le backend
cd backend && npm ci && npm start
```

```bash
# Un terminal pour le frontend
cd frontend && npm ci && npm start
```

**http://localhost:5173/**
